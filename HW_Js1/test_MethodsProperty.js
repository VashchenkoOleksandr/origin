// Заготовка під тести:

'use strict';
test('test_string', () => {
    expect().toEqual();
});

test('test_numbers', () => {
    expect().toEqual();
});

test('test_arrays', () => {
    expect().toEqual();
});

test('test_compare_operators', () => {
    expect().toEqual();
});

test('test_equality_comparison_algorithm', () => {
    expect().toEqual();
});
